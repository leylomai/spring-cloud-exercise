server:
  port: 1001
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    #service-url:
      #defaultZone: http://localhost:8888/eureka
  instance:
    hostname: localhost
spring:
  application:
    name: eureka-server


